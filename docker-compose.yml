services:
  backend:
    build: ./backend
    volumes:
      - ./campaigns.json:/data/campaigns.json:ro,z
      - ./cache:/data/cache:z
      - ./linkedin_ads/token.json:/data/token.json:ro,z
    ports:
      - "${BACKEND_PORT:-9016}:8000"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: /api
    ports:
      - "${FRONTEND_PORT:-9015}:80"
    depends_on:
      - backend
    restart: unless-stopped
